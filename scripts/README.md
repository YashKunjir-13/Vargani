# System & Automation Scripts (`scripts/`)

Operational scripts for database seeding, environment provisioning, build verification, and deployment helpers.

## Available Scripts

- `seed.ts`: Database seeding for development tenants and sample data.
- `migrate.sh`: Automated migration deployment wrapper.
- `build-all.sh`: Monorepo build and validation trigger.
